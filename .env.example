# Configurações da UG (exemplo)
UASG=160517
TIMEZONE=America/Sao_Paulo

# Ajustes de rede/cache
CACHE_TTL=600
MAX_RETRIES=6
BASE_BACKOFF=1.0
# Forçar IPv4 (v4), IPv6 (v6) ou deixar automático (auto)
HTTP_IPRESOLVE=v4
# Proxy HTTP(S) opcional (ex.: http://usuario:senha@proxy:3128). Deixe vazio se não usar.
HTTP_PROXY_URL=
HTTP_PROXY_USERPWD=
HTTP_NOPROXY=
